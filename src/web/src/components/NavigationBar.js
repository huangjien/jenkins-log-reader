import React from 'react';

const NavigationBar = () => {
  return (
    <div className="bg-blue-500 text-white p-4">
      <input type="search" placeholder="Search..." className="p-2 rounded"/>
      {/* Add more buttons as needed */}
    </div>
  );
};

export default NavigationBar;
